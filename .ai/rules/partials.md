---
paths:
  - 'resources/js/registerServiceWorker.js,resources/views/partials/head.blade.php'
---

# Partials

## No automatic PWA install prompt
Do not show a sitewide PWA install banner or visit-based install prompt. Users can still install via the browser UI; iOS notification settings may instruct Add to Home Screen when push is unavailable in Safari.
